{{-- <p>Click the link below to verify your email address:</p>
<a href="{{ url('api/v1/verify-email/' . $token) }}">Verify Email</a> --}}


<p>Click the link below to verify your email address:</p>
<a href="{{ $token }}">Verify Email</a>
